# Copy this to .env on the VPS and fill in real values.
# Never commit .env to git.

# Admin panel password  (change this!)
ADMIN_PASSWORD=change-me-now

# M-Pesa (leave blank if not using payments)
MPESA_CONSUMER_KEY=
MPESA_CONSUMER_SECRET=
MPESA_SHORTCODE=
MPESA_PASSKEY=
MPESA_ENV=sandbox
